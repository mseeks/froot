# doc-coherence smoke test (temporary)

A throwaway PR to watch the new **doc-coherence** agentic loop reason about docs
vs. code. Safe to close.

These claims are deliberately wrong — the agent should read the code and catch
them:

- froot's changelog judge runs on **OpenAI GPT-4** by default. (Check the model
  configuration in the source.)
- froot manages **exactly three** maintenance loops in total.
