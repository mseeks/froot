# doc-refs smoke test (temporary)

A throwaway PR to watch the new **doc-refs** advisory loop in action. Safe to
close.

It links to a file that does not exist: [the missing guide](docs/this-does-not-exist.md).

It also mentions a backtick path that is gone: `src/froot/totally_gone.py`.

And references a make target that does not exist: run `make nonexistent-target`.
